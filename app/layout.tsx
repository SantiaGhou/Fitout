import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'FitOut - Transforme seu corpo com IA',
  description: 'Plataforma de treinos com inteligência artificial que entrega planos personalizados sincronizados com dieta adaptada ao seu progresso.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
